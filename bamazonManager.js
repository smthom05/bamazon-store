// Need to add code for deleting a product, updating stock quantity, and a menu

const mysql = require('mysql');
const inquirer = require('inquirer');
const connection = require('./connection.js');

// Test if github is connected
